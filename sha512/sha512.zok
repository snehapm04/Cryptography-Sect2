import "1024bitPacked.zok" as sha512packed; 
def main( private field a, private field b, private field c, private field d, private field e, private field f, private field g, private field h, field h0, field h1, field h2, field h3)
{
field[4] ans = sha512packed([a, b, c, d, e, f, g, h]); 
assert(ans [0] ==h0);
assert(ans [1] == h1);
assert(ans[2] == h2);
assert(ans[3] == h3);
return;
}